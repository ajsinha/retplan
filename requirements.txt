# RetPlan runtime dependencies.
#
# The engine needs only numpy. Everything else is the web application; the
# LibreOffice workbook path additionally needs python3-uno, which ships with
# LibreOffice and is not installable from PyPI.

numpy>=1.26

# Web application (FastAPI + uvicorn, Jinja2 templates, session middleware)
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
itsdangerous>=2.1
python-multipart>=0.0.9
